# Notas

``` help
    Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Numero que va a ser multiplicado.          [número] [requerido]
  -l, --list     Muestra la tabla en pantalla.       [booleano] [defecto: false]
  -h, --hasta    Determina hasta que número llega la tabla[número] [defecto: 10]
```
